expose_php = Off
memory_limit = 256M
upload_max_filesize = 10M
post_max_size = 10M

date.timezone = UTC
opcache.memory_consumption = 128
opcache.max_accelerated_files = 20000
; The image is immutable, so PHP never needs to re-stat source files.
opcache.validate_timestamps = 0
